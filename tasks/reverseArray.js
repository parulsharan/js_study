// how to reverse an array
//input =[3,5,2,5,1,6]//
const reverseArray = (input) => {
  for (let i = input.length - 1; i >= 0; i--) {
    console.log(`xxx: ${input[i]}`);
  }
};
const input = [1, 2, 3, 4, 5];
reverseArray(input);
// console.log(`the reverse array is: ${result}`);
